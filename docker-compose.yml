services:
  colonyforge-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./Vault:/app/Vault
      - ./colonyforge.config.yaml:/app/colonyforge.config.yaml:ro
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - COLONYFORGE_API_KEY=${COLONYFORGE_API_KEY:-}
    command: ["colonyforge", "server", "--host", "0.0.0.0", "--port", "8000"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Playwright MCP Server（ローカルDockerfile使用）
  playwright-mcp:
    build:
      context: .devcontainer
      dockerfile: Dockerfile.playwright-mcp
    ports:
      - "8931:8931"
    volumes:
      - playwright_output:/tmp/playwright-output
    environment:
      - PLAYWRIGHT_MCP_HOST=0.0.0.0
      - PLAYWRIGHT_MCP_PORT=8931
    # 公式イメージはENTRYPOINTで--headless --browser chromium --no-sandbox設定済み
    # --portでHTTPトランスポートを有効化
    command: ["--port", "8931", "--host", "0.0.0.0"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8931/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # オプション: Redis (将来のスケーリング用)
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data

volumes:
  vault_data:
  playwright_output:
  # redis_data:
