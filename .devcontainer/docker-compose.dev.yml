services:
  colonyforge-dev:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile.dev
    volumes:
      # WSL Docker環境用：Windowsパスを/mnt/形式に変換
      # Docker Desktop with WSL backend では /mnt/d/... 形式が必要
      - /mnt/d/projects/biz/ColonyForge:/workspace/ColonyForge:cached
      # .venvは/home/vscode/.venv に配置してpipのユーザーインストールと分離
      - colonyforge-venv:/home/vscode/.venv
      - /var/run/docker.sock:/var/run/docker.sock
    command: sleep infinity
    user: vscode
    group_add:
      - docker
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - OLLAMA_HOST=http://ollama:11434
    networks:
      - colonyforge-dev-net

  # GPU対応Ollama（GPUがある場合のみ起動）
  ollama:
    image: ollama/ollama:latest
    container_name: colonyforge-dev-ollama
    profiles:
      - gpu
    ports:
      - "11434:11434"
    volumes:
      - ollama_models:/root/.ollama
    restart: unless-stopped
    networks:
      colonyforge-dev-net:
        aliases:
          - ollama
          - colonyforge-dev-ollama
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  # CPU版Ollama（GPUがない場合のフォールバック）
  ollama-cpu:
    image: ollama/ollama:latest
    container_name: colonyforge-dev-ollama-cpu
    profiles:
      - cpu
    ports:
      - "11434:11434"
    volumes:
      - ollama_models:/root/.ollama
    restart: unless-stopped
    networks:
      colonyforge-dev-net:
        aliases:
          - ollama
          - colonyforge-dev-ollama

  # code-server: ブラウザ版VS Code（VLMテスト対象）
  code-server:
    image: codercom/code-server:latest
    container_name: colonyforge-code-server
    ports:
      - "8080:8080"
    environment:
      - PASSWORD=colonyforge
    volumes:
      # 名前付きボリュームを使用（Windowsパス問題を回避）
      - colonyforge-workspace:/workspace/ColonyForge
    restart: unless-stopped
    networks:
      - colonyforge-dev-net

  # Playwright MCP Server（ローカルDockerfile使用）
  # ローカルにPlaywrightをインストールせずにMCP経由でブラウザ操作可能
  # socat で localhost:8080 → code-server:8080 をフォワード
  # → ブラウザがlocalhostアクセス → secure context → ServiceWorker動作
  playwright-mcp:
    build:
      context: .
      dockerfile: Dockerfile.playwright-mcp
    container_name: colonyforge-playwright-mcp
    ports:
      - "8931:8931"
    tmpfs:
      - /tmp/playwright-output:mode=1777
    environment:
      - PLAYWRIGHT_MCP_NO_SANDBOX=true
      - PLAYWRIGHT_MCP_VIEWPORT_SIZE=1920x1080
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        socat TCP-LISTEN:8080,fork,reuseaddr TCP:colonyforge-code-server:8080 &
        exec npx @playwright/mcp@latest --port 8931 --host 0.0.0.0 --no-sandbox --browser chromium --viewport-size=1920x1080 --allowed-hosts '*'
    restart: unless-stopped
    networks:
      colonyforge-dev-net:
        aliases:
          - playwright-mcp
          - colonyforge-playwright-mcp

volumes:
  colonyforge-venv:
  colonyforge-workspace:
    # code-server用のワークスペースボリューム
  ollama_models:
    name: colonyforge_ollama_models

networks:
  colonyforge-dev-net:
    name: colonyforge-dev-network
